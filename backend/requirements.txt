# Core API
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Config & validation
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.0

# Vector database (includes ONNX embedding function)
chromadb>=0.5.0
onnxruntime>=1.18.0

# Keyword retrieval
rank-bm25>=0.2.2

# LLM — HuggingFace local fallback (transformers + torch)
# NOTE: if torch fails to load on Windows, use Groq or Ollama instead.
# Install CPU-only torch if needed:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
transformers>=4.41.0
torch>=2.0.0
sentence-transformers>=3.0.0

# HTTP client (Groq API, Ollama)
httpx>=0.27.0

# Wikipedia data ingestion
wikipedia>=1.4.0

# Utilities
numpy>=1.26.0
